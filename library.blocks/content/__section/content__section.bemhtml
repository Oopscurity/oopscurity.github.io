block('content').elem('section')(
	tag()('section'),

	content()(function() {
		return [
			{
				elem: 'layer',
				mix: [
					{ block: 'layer' },
					{ block: 'clearfix' }
				],
				content: this.ctx.content
			}
		]
	})
)